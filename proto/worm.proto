syntax = "proto3";

package boundless.services;

// service definition.
service WormService {
  // Sends a command
  rpc SendCommand(Command) returns (Response) {}
}

// The response from the service.
message Response {
  string message = 1;
}
